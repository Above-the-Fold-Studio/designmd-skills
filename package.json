{
  "name": "@design-skills/catalog",
  "version": "0.0.0",
  "private": true,
  "description": "Validation and catalog generation for portable design skills.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "catalog": "node scripts/generate-catalog.mjs",
    "catalog:check": "node scripts/generate-catalog.mjs --check",
    "validate": "node scripts/validate.mjs",
    "test": "node --test",
    "check": "npm run validate && npm run catalog:check && npm test"
  },
  "license": "MIT",
  "devDependencies": {
    "ajv": "^8.18.0"
  }
}
